variable x:integer;
variable y:boolean;
variable y:boolean;
variable y:boolean;
constant a:integer;
constant b:boolean;
variable i:integer;

main
begin
    b(something, here, i); // not a function
    test_func(here, and, there); // function not defined
    x:=3 + 3;
    i := true;
end