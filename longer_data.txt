integer some_func (x:integer, y:integer) is
    variable result:integer;
    variable minus_sign: boolean;

    begin
        if true < false
        begin
            minus_sign := true;
            x := x + 3;
            b := true;
            /* x := -x - 3; */
            /* x := x + 3; */
        end
        else
        begin
            if (x > 0) 
            begin
                skip;
            end
        end
    return(minus_sign);
end